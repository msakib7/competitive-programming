#include<bits/stdc++.h>
using namespace std;
int main()
{
    string arr;
    while(cin>>arr)
    {
        int len;
        len=size.arr();
        for(int i=0;i<len;i++)
        {
            if(arr[i]=='.')
                cnt1++;
            if(cnt>mx)
                mx=cnt;
            if(arr[i]=='X')
                cnt1=0;

        }
    }
}
